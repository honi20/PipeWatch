import UpdatePasswordCard from "@src/components/manageAccount/UpdatePasswordCard";

export const UpdatePassword = () => {
  return (
    <div
      className="flex items-center justify-center w-full"
      style={{ height: "calc(100vh - 220px)" }}
    >
      <UpdatePasswordCard />
    </div>
  );
};
