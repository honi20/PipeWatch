import EditInfoCard from "@src/components/manageAccount/EditInfoCard";

export const EditInfo = () => {
  return (
    <div
      className="flex items-center justify-center w-full"
      style={{ height: "calc(100vh - 220px)" }}
    >
      <EditInfoCard />
    </div>
  );
};
