import Dashboard from "@src/components/manageAccount/dashboard";

export const ManageAccount = () => {
  return (
    <div
      className="flex items-center justify-center w-full h-full"
      // style={{ height: "calc(100vh - 220px)" }}
    >
      <Dashboard />
    </div>
  );
};
